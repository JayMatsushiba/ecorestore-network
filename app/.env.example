# Ecorestore Network — React app environment
#
# Optional. Defaults to http://localhost:4000 (server/index.ts's default
# port) if unset — see src/api/client.ts. No secrets belong in this file;
# this app never holds an API key, private key, or RPC credential.
VITE_API_BASE_URL=http://localhost:4000
